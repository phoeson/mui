<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:FirstFloor.ModernUI.Windows.Controls">

    <!-- ═══════════════════════════════════════════════════════════════════════
         StatusBadge
         A compact pill-shaped tag for displaying order / shipment status.
         Solid background with high-contrast white (or dark-on-amber) text.

         Usage:
             <mui:StatusBadge Severity="Success">Delivered</mui:StatusBadge>
             <mui:StatusBadge Severity="Warning">Pending</mui:StatusBadge>
             <mui:StatusBadge Severity="Error">Cancelled</mui:StatusBadge>
    ═══════════════════════════════════════════════════════════════════════ -->

    <Style TargetType="controls:StatusBadge">
        <Setter Property="FocusVisualStyle"           Value="{x:Null}"/>
        <Setter Property="FontSize"                   Value="11"/>
        <Setter Property="FontWeight"                 Value="SemiBold"/>
        <Setter Property="Padding"                    Value="8,3"/>
        <!-- Default (neutral grey) foreground — white on grey -->
        <Setter Property="Foreground"                 Value="#FFFFFF"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment"   Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:StatusBadge">
                    <!-- Default: neutral grey pill -->
                    <Border x:Name="Chrome"
                            Background="#6E767D"
                            BorderThickness="0"
                            CornerRadius="10"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          TextBlock.FontSize="{TemplateBinding FontSize}"
                                          TextBlock.FontWeight="{TemplateBinding FontWeight}"
                                          TextBlock.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>

                        <!-- Information — Microsoft blue, white text -->
                        <Trigger Property="Severity" Value="Information">
                            <Setter TargetName="Chrome" Property="Background" Value="#0078D4"/>
                            <Setter Property="Foreground"                      Value="#FFFFFF"/>
                        </Trigger>

                        <!-- Success — Microsoft green, white text -->
                        <Trigger Property="Severity" Value="Success">
                            <Setter TargetName="Chrome" Property="Background" Value="#107C10"/>
                            <Setter Property="Foreground"                      Value="#FFFFFF"/>
                        </Trigger>

                        <!-- Warning — amber; amber is high-luminance so use dark text -->
                        <Trigger Property="Severity" Value="Warning">
                            <Setter TargetName="Chrome" Property="Background" Value="#F0A000"/>
                            <Setter Property="Foreground"                      Value="#1A1A1A"/>
                        </Trigger>

                        <!-- Error — Microsoft red, white text -->
                        <Trigger Property="Severity" Value="Error">
                            <Setter TargetName="Chrome" Property="Background" Value="#E81123"/>
                            <Setter Property="Foreground"                      Value="#FFFFFF"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
