<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:FirstFloor.ModernUI.Windows.Controls">

    <!-- ═══════════════════════════════════════════════════════════════════════
         ButtonGroup
         A horizontal strip of action buttons sharing a continuous border.
         Corner radii are applied automatically by the Position property.

         Usage:
             <mui:ButtonGroup>
                 <mui:ButtonGroupItem Content="Receive" Command="{Binding ...}"/>
                 <mui:ButtonGroupItem Content="Ship"    Command="{Binding ...}"/>
                 <mui:ButtonGroupItem Content="Return"  Command="{Binding ...}"/>
             </mui:ButtonGroup>
    ═══════════════════════════════════════════════════════════════════════ -->

    <!-- Container: a simple horizontal ItemsPresenter with no chrome of its own -->
    <Style TargetType="controls:ButtonGroup">
        <Setter Property="Background"  Value="Transparent"/>
        <Setter Property="Focusable"   Value="False"/>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:ButtonGroup">
                    <ItemsPresenter SnapsToDevicePixels="True"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Item: a ButtonBase with position-driven corner radii -->
    <Style TargetType="controls:ButtonGroupItem">
        <Setter Property="FocusVisualStyle"          Value="{x:Null}"/>
        <Setter Property="Cursor"                    Value="Hand"/>
        <Setter Property="Background"                Value="{DynamicResource ButtonBackground}"/>
        <Setter Property="BorderBrush"               Value="{DynamicResource ButtonBorder}"/>
        <Setter Property="BorderThickness"           Value="1"/>
        <Setter Property="Foreground"                Value="{DynamicResource ButtonText}"/>
        <Setter Property="Padding"                   Value="16,5"/>
        <Setter Property="MinHeight"                 Value="28"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment"   Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:ButtonGroupItem">
                    <Border x:Name="Chrome"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>
                    <ControlTemplate.Triggers>

                        <!-- ── Position → corner radii and border sides ── -->
                        <Trigger Property="Position" Value="Only">
                            <Setter TargetName="Chrome" Property="CornerRadius"    Value="4"/>
                            <Setter Property="BorderThickness"                     Value="1"/>
                        </Trigger>
                        <Trigger Property="Position" Value="First">
                            <Setter TargetName="Chrome" Property="CornerRadius"    Value="4,0,0,4"/>
                            <Setter Property="BorderThickness"                     Value="1"/>
                        </Trigger>
                        <Trigger Property="Position" Value="Middle">
                            <Setter TargetName="Chrome" Property="CornerRadius"    Value="0"/>
                            <!-- Collapse the left border to avoid a double-width divider -->
                            <Setter Property="BorderThickness"                     Value="0,1,1,1"/>
                        </Trigger>
                        <Trigger Property="Position" Value="Last">
                            <Setter TargetName="Chrome" Property="CornerRadius"    Value="0,4,4,0"/>
                            <Setter Property="BorderThickness"                     Value="0,1,1,1"/>
                        </Trigger>

                        <!-- ── Interaction states ── -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Chrome" Property="Background"   Value="{DynamicResource ButtonBackgroundHover}"/>
                            <Setter TargetName="Chrome" Property="BorderBrush"  Value="{DynamicResource ButtonBorderHover}"/>
                            <Setter Property="Foreground"                        Value="{DynamicResource ButtonTextHover}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Chrome" Property="Background"   Value="{DynamicResource ButtonBackgroundPressed}"/>
                            <Setter TargetName="Chrome" Property="BorderBrush"  Value="{DynamicResource ButtonBorderPressed}"/>
                            <Setter Property="Foreground"                        Value="{DynamicResource ButtonTextPressed}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground"                        Value="{DynamicResource ButtonTextDisabled}"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="Chrome" Property="BorderBrush"  Value="{DynamicResource Accent}"/>
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
